# Example environment file for Relapse (DO NOT commit secrets)
# Copy this to `.env` and fill in real values for local development.

# Database (optional) - default uses SQLite file `relapse.db`
# Example: DATABASE_URL=sqlite:///relapse.db
DATABASE_URL=sqlite:///relapse.db

# Google Cloud Storage bucket for photo uploads
# Example: GCP_BUCKET=relapse-photos-bucket
GCP_BUCKET=your-gcs-bucket-name

# Path to service account JSON (optional). If not set, Application Default
# Credentials (ADC) will be used when available.
# Example: GOOGLE_APPLICATION_CREDENTIALS=/home/solomon/.config/gcloud/relapse-sa.json
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Flask settings
FLASK_APP=app:create_app
FLASK_DEBUG=1

# Optional: port to run the app on
PORT=5000
